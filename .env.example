# =============================================================================
# Raumbuchungssystem - Umgebungsvariablen Beispiel
# =============================================================================
#
# ANLEITUNG:
# 1. Kopiere diese Datei nach .env:
#    cp .env.example .env        (Linux/Mac)
#    copy .env.example .env      (Windows)
#
# 2. Passe die Werte in .env an (NICHT in dieser Datei!)
#
# 3. WICHTIG: Setze SICHERE Passwörter und einen neuen SECRET_KEY!
#
# =============================================================================

# -----------------------------------------------------------------------------
# Flask Konfiguration
# -----------------------------------------------------------------------------

# Secret Key für Session-Verschlüsselung (MUSS geändert werden!)
# Generiere mit: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=HIER-EINEN-NEUEN-SICHEREN-KEY-GENERIEREN

# Flask Environment (development oder production)
FLASK_ENV=production

# -----------------------------------------------------------------------------
# Datenbank Konfiguration
# -----------------------------------------------------------------------------

# PostgreSQL Passwort (MUSS geändert werden!)
DB_PASSWORD=HIER-EIN-SICHERES-PASSWORT

# Hinweis: DATABASE_URI wird automatisch in docker-compose.yml gesetzt
# Für lokale Entwicklung ohne Docker:
# DATABASE_URI=postgresql://admin:DEIN-PASSWORT@localhost:5433/buchungen

# -----------------------------------------------------------------------------
# E-Mail Konfiguration
# -----------------------------------------------------------------------------

# SMTP Server
MAIL_SERVER=your-mail-server.com
MAIL_PORT=25
MAIL_USE_TLS=False
MAIL_USE_SSL=False

# SMTP Authentifizierung
MAIL_USERNAME=your-smtp-username
MAIL_PASSWORD=your-smtp-password

# Absender
MAIL_DEFAULT_SENDER_NAME=Your Organization Name
MAIL_DEFAULT_SENDER_EMAIL=noreply@example.com

# -----------------------------------------------------------------------------
# Admin Konfiguration
# -----------------------------------------------------------------------------

# Admin E-Mail (erhält Benachrichtigungen)
ADMIN_EMAIL=admin@example.com

# Admin PIN für Web-Interface (SOLLTE durch längeres Passwort ersetzt werden!)
ADMIN_PIN=your-secure-pin-here

# -----------------------------------------------------------------------------
# Optional: pgAdmin Konfiguration
# -----------------------------------------------------------------------------

# pgAdmin Web-Interface Passwort
PGADMIN_PASSWORD=admin
